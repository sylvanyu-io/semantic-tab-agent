{
  "name": "tab-recap",
  "version": "0.3.3",
  "private": true,
  "type": "module",
  "description": "AI tab organizer for Chrome with private work recaps from local tab activity.",
  "scripts": {
    "build": "npm run build:extension",
    "test": "node --test tests/*.test.mjs worker/test/*.test.mjs",
    "test:worker": "node --test worker/test/*.test.mjs",
    "test:ui": "playwright test tests/ui-smoke.spec.mjs",
    "smoke:gateway": "node scripts/live-smoke-gateway.mjs",
    "smoke:long-recap": "node scripts/smoke-long-recap-lifecycle.mjs",
    "benchmark:planner-scale": "node scripts/benchmark-gateway-planner-scale.mjs",
    "analyze:planner-quality": "node scripts/analyze-planner-benchmark-quality.mjs",
    "benchmark:recap-extension-scale": "node scripts/benchmark-recap-extension-scale.mjs",
    "stress:extension": "node scripts/stress-extension.mjs",
    "stress:summary": "node scripts/summarize-stress-artifact.mjs",
    "assets:icons": "node scripts/generate-icons.mjs",
    "assets:readme": "node scripts/generate-readme-assets.mjs",
    "clean:dist": "node scripts/clean-dist.mjs",
    "audit:release-artifacts": "node scripts/audit-release-artifacts.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "scan:secrets:history": "node scripts/scan-secrets-history.mjs",
    "build:extension": "node scripts/build-extension.mjs",
    "build:extension:store": "EXTENSION_CHANNEL=store node scripts/build-extension.mjs",
    "check": "npm test && npm run test:ui",
    "release:check": "npm run clean:dist && npm run assets:icons && npm run check && npm run scan:secrets && npm run scan:secrets:history && npm run build:extension && npm run build:extension:store && npm run audit:release-artifacts",
    "release:check:full": "npm run release:check && npm run stress:extension",
    "release:publish-check": "node scripts/verify-release-version.mjs && npm run release:check:full",
    "release:check:live": "node scripts/require-monitor-token.mjs && STRESS_GATEWAY=1 npm run release:check:full && GATEWAY_REQUIRE_MONITOR=1 npm run smoke:gateway"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
